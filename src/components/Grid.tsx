import React from 'react';

class Grid extends React.Component{

    constructor(props : {}){
        super(props);

        this.state = {
            
        };
    }

    render(): React.ReactNode {
        return (
            <div></div>
        );
    }
}

export default Grid;